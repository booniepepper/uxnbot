#!/bin/sh

set -eu

echo "$@" >temp.tal

# stderr redirection and exits are temporary until I figure out how to handle
# them in the Factor code for Discord
2>&1 uxnasm temp.tal temp.rom || exit 0
2>&1 uxnbot temp.rom || exit 0
